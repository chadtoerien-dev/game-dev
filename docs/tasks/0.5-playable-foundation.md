# Task Brief — Playable Foundation 0.5

## Title

Implement Playable Foundation 0.5: third-person character, camera and input

**Status:** Complete — accepted by Chad Toerien on 19 July 2026

## Player and production value

The native social simulation builds and tests, and the saved sandbox contains the structurally validated and human-accepted playable shell required to test later interaction, social, Tribunal and night-operation systems in context.

## Objective

When `/Game/Maps/L_Dev_Sandbox` starts in Play-in-Editor, spawn a controlled third-person character with responsive camera-relative movement, mouse and controller look, sprint, crouch and jump.

## Current behaviour

- Unreal Engine 5.8 builds and opens the project.
- `L_Dev_Sandbox` loads and can enter and exit PIE without a fatal error.
- All twelve current `TheVeil.*` automation tests pass, including the five pre-existing social tests and seven playable-foundation tests.
- The native player character, PlayerController and GameMode are implemented on the feature branch.
- The manifest-owned Enhanced Input assets and derived Blueprints are persisted locally and pass strict structural validation.
- `L_Dev_Sandbox` has the intended GameMode override, one loaded PlayerStart and no placed auto-possessed pawn.
- The full keyboard/controller, camera, focus-loss, spawn-clearance and stability checklist was accepted by the Product Owner on 19 July 2026.

## Required behaviour

- `L_Dev_Sandbox` uses a deliberate The Veil GameMode.
- A The Veil player character spawns at a PlayerStart.
- WASD and left stick provide camera-relative movement.
- Mouse and right stick control the camera.
- The character can sprint, crouch and jump.
- Movement acceleration, braking and rotation are responsive rather than slow or clunky.
- The camera handles collision and does not visibly judder under normal PIE conditions.
- The existing social simulation and automation tests remain unaffected.

## Read first

- `AGENTS.md`
- `docs/context/AGENT_HANDOFF.md`
- `docs/production/ROADMAP.md`
- `docs/production/AI_AGENT_WORKFLOW.md`
- `docs/technical/TECHNICAL_ARCHITECTURE.md`
- `docs/design/VERTICAL_SLICE.md`
- `INTEGRATION.md`
- `Source/TheVeil/TheVeil.Build.cs`
- existing source and tests under `Source/TheVeil/`
- current `Config/DefaultEngine.ini` and `Config/DefaultInput.ini`

## Preconditions

- Local `main` and `origin/main` are reconciled.
- The UE5.8 source fixes are on `main`.
- `L_Dev_Sandbox` and its complete World Partition companion assets are on `main`.
- `scripts/Build-TheVeil.ps1` succeeds.
- `scripts/Run-TheVeilTests.ps1` confirms the existing five tests pass.

## In scope

- one native player character class
- one native PlayerController if required for clean input/camera ownership
- one native GameMode
- Enhanced Input actions and one mapping context created through a bounded Unreal Editor commandlet
- camera boom and follow camera
- camera-relative walking
- sprint, crouch and jump
- movement tuning values exposed for development iteration
- one PlayerStart in the sandbox
- development-only movement diagnostics where useful
- focused automation or functional tests where technically practical
- integration documentation for all editor-created assets

## Out of scope

- final character model or costume
- final locomotion animation set
- motion matching
- parkour, climbing or mantling
- combat
- inventory
- conversation UI
- NPC navigation or StateTrees
- Tribunal presentation
- multiplayer or replication
- final UI, audio, music, lighting or cinematic polish
- browser, mobile, console or VR support

## Constraints

- Unreal Engine 5.8
- Windows x64
- MSVC toolchain `14.50.35717`
- use Enhanced Input rather than introducing new legacy action mappings
- British English for documentation and player-facing text
- preserve all existing social-system behaviour and tests
- no broad refactor of the social, memory, claim, voting or save systems
- do not hand-edit `.uasset`, `.umap`, World Partition external actor/object files or HLOD assets
- never hand-edit Unreal binary assets; use the guarded Unreal Editor commandlet or an explicit human Editor session
- no unrelated config churn

## Implemented source files

The bounded native implementation uses:

- `Source/TheVeil/Characters/TheVeilCharacter.h`
- `Source/TheVeil/Characters/TheVeilCharacter.cpp`
- `Source/TheVeil/Core/TheVeilPlayerController.h`
- `Source/TheVeil/Core/TheVeilPlayerController.cpp`
- `Source/TheVeil/Core/TheVeilGameMode.h`
- `Source/TheVeil/Core/TheVeilGameMode.cpp`
- `Source/TheVeil/TheVeil.Build.cs` only when a verified dependency is missing
- one or more focused test files under `Source/TheVeil/Tests/`
- `INTEGRATION.md`

Editor-created assets, generated at the exact paths below by the checked manifest after source compilation:

- `/Game/TheVeil/Input/IA_Move`
- `/Game/TheVeil/Input/IA_Look`
- `/Game/TheVeil/Input/IA_Jump`
- `/Game/TheVeil/Input/IA_Sprint`
- `/Game/TheVeil/Input/IA_Crouch`
- `/Game/TheVeil/Input/IMC_Player`
- `/Game/TheVeil/Characters/BP_TheVeilCharacter`
- `/Game/TheVeil/Core/BP_TheVeilPlayerController`
- `/Game/TheVeil/Core/BP_TheVeilGameMode`

Any additional source file requires an explanation before editing. Binary assets must be changed serially through Unreal's APIs, never by parallel agents or file-level editing.

## Initial tuning target

These are starting values, not immutable design decisions:

- walk speed: approximately 450–520 cm/s
- sprint speed: approximately 680–760 cm/s
- crouched speed: approximately 250–320 cm/s
- acceleration: approximately 1800–2400 cm/s²
- braking deceleration: approximately 1600–2200 cm/s²
- rotation rate: approximately 500–720 degrees/s
- spring-arm length: approximately 300–380 cm

The implementer must expose or centralise tuning so the human can adjust feel in editor or defaults without rewriting input logic.

## Acceptance criteria

1. The editor target builds successfully with no new compiler errors.
2. All twelve current `TheVeil.*` automation tests remain green, including the five tests that pre-date this milestone.
3. `L_Dev_Sandbox` starts with the intended GameMode.
4. Exactly one local player pawn spawns at the intended PlayerStart.
5. Keyboard movement is camera-relative and responsive.
6. Controller movement and look have functional parity.
7. Sprint starts and stops reliably and cannot remain stuck after key release or focus loss.
8. Crouch transitions reliably and respects collision constraints.
9. Jump works only when allowed by normal Character Movement rules.
10. The camera rotates smoothly, follows the character and retracts against geometry.
11. The character does not fall through the landscape or spawn inside geometry.
12. PIE can run for at least two minutes and exit with `Esc` without a crash or fatal error.
13. The implementation does not modify social-state, memory, claim, voting or save behaviour.
14. A short capture demonstrates walking, sprinting, crouching, jumping, camera rotation and collision.
15. Integration documentation lists every editor-created asset and its settings.

Acceptance disposition: criteria 1–13 and 15 passed. For criterion 14, the Product Owner accepted the live visual session in place of a retained recording and instructed the project to proceed; no video artefact is claimed.

## Verification

Build:

```powershell
powershell -ExecutionPolicy Bypass -File ".\scripts\Build-TheVeil.ps1"
```

Automation:

```powershell
powershell -ExecutionPolicy Bypass -File ".\scripts\Run-TheVeilTests.ps1"
```

Repository checks:

```powershell
git diff --check
git status --short
```

Manual PIE test:

1. Open `L_Dev_Sandbox`.
2. Confirm the expected GameMode and default pawn.
3. Press Play in Selected Viewport.
4. Test WASD, mouse look, sprint, crouch and jump.
5. Repeat movement and camera checks with a controller.
6. Walk the character into terrain and test camera obstruction.
7. Alt-tab or change viewport focus while sprint is held, then return and confirm sprint is not stuck.
8. Run for at least two minutes.
9. Press `Esc` and confirm clean PIE shutdown.
10. Capture video and screenshots for Gemini visual review.

## Risks

- Input can be initialised in the wrong lifecycle function or applied repeatedly.
- A Blueprint default pawn may silently override the native GameMode setting.
- World Partition or PlayerStart placement can create apparent movement failures unrelated to C++.
- Editor-generated input assets can be omitted from Git if the commit is not reviewed carefully.
- Over-tuning movement before animation selection can waste work.
- Parallel agents can conflict in character, controller, GameMode and config files.

## Rollback

- Revert the feature branch or PR.
- Restore the previous map GameMode override and PlayerStart state in Unreal Editor.
- Do not delete or regenerate the sandbox World Partition files manually.
- Preserve the verified 0.4 source and tests.

## Agent routing

- Planner: GPT-5.6 Sol at High or XHigh reasoning
- Primary implementer: Claude Sonnet 5 in Cursor or Claude Code
- Bounded script/test follow-up: Cursor Composer 2.5
- Independent code reviewer: GPT-5.6 Sol or DeepSeek V4 Pro, whichever did not implement
- Multimodal PIE reviewer: Gemini 3.5 Flash or current Gemini Pro
- Human editor and acceptance owner: Chad Toerien

## Parallelisation boundary

Do not run multiple implementation agents against the same character, controller, GameMode, input or config files.

Safe parallel work after interfaces are approved:

- one agent implements native character/controller/GameMode
- one independent agent drafts tests without editing implementation files
- Gemini prepares a visual review checklist
- the guarded commandlet creates and wires editor assets after the native build passes

## Definition of done

This task is complete. The source and content diff, build and automation extracts, structural validation, live PIE observations, Product Owner controller/camera/focus-loss/visual attestation, integration notes and independent reviews are recorded in the pull request evidence. The live acceptance session was not retained as a video file; `docs/evidence/0.5-playable-foundation-human-acceptance.md` records that limitation without inventing capture evidence.
